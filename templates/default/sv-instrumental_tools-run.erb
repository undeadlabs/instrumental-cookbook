#!/bin/sh

PATH=/opt/chef/embedded/bin:$PATH

exec 2>&1
exec <%= node[:runit][:chpst_bin] %> \
      -u <%= node[:instrumental][:tools][:user] %>:<%= node[:instrumental][:tools][:group] %> \
      instrument_server -k <%= node[:instrumental][:api_key] %> -H <%= node[:instrumental][:tools][:hostname] %>
